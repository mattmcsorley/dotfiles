- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zsh/zshrc
    ~/.gitignore_global: git/gitignore_global
- shell:
  - [git submodule update --init --recursive, Installing submodules]

